<div class='fieldset {{#if error}}error{{/if}}'>
  <label for='{{name}}'>{{label}}</label>
  <input type='{{type}}' name='{{name}}' id='{{name}}' value='{{value}}' />

  {{#if error}}
    <span class='error-message'>{{error}}</span>
  {{/if}}
</div>
