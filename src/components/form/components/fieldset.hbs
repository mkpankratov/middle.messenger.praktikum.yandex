<div class='fieldset'>
  <label for='{{name}}'>{{label}}</label>
  <input type='{{type}}' name='{{name}}' id='{{name}}' value='{{value}}' />

  {{#if error}}
    <span class='error'>{{error}}</span>
  {{/if}}
</div>